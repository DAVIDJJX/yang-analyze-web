/* =========================================================================
 * Yang-analyze web — db/noise_db.js
 * 噪音模組對照表（預設值）。
 * 依據：檢驗室噪音 24hr 檢測報告（N-xx 工作表）＋環境振動測定報告（V-xx）
 *       與環境部噪音資料辭典（docs/noise_module_spec.md）。
 * ========================================================================= */
(function () {
  "use strict";
  window.YangDB = window.YangDB || {};

  YangDB.noise = {
    /* 內建預設對照表版本（更動任一預設表內容時 +1，觸發合併提示） */
    tablesVersion: 1,

    /* 官方檢測類別（七種） */
    categories: ["環境噪音", "道路交通噪音", "營建工程噪音", "公私場所噪音", "航空噪音", "低頻噪音", "振動"],

    /* 申報常數（「對照表編輯」可改）
       頻率範圍(噪音)：照歷次申報慣例填「20 Hz 至 200 kHz」
       （官方辭典選項寫 20 Hz 至 20kHz，經使用者確認維持照舊）。 */
    constMap: {
      "管制標準": "噪音管制法第7條第1項",
      "頻率範圍(噪音)": "20 Hz 至 200 kHz"
    },

    /* 監測單位代碼（官方代碼表：16=db(A)、159=dB）與振動固定值 */
    unitNoise: 16,
    unitVib: 159
  };
})();
